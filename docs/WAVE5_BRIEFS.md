# Wave 5 builder briefs (lead: 2026-09-10)

Read first: `docs/REPAIR_PLAN.md` Part A (every rule applies, verbatim), `docs/HANDOFF_WAVE5_CALL_POLICIES.md`, `docs/CALL_POLICIES_DRAFT.md` "Founder decisions", `ARCHITECTURE.md` 5, 6.6, 6.7, 6.8. Work ONLY in `/Users/ashergoldstein/Projects/expertmatch/.claude/worktrees/wave5` (branch `fix/wave5`). Never commit. Never read `.env.local`. No new dependencies. Report with the exact Part A4 headings.

Lead has already landed, for everyone (do not edit): `types.ts` (BookingState `cancelledAt/cancelledBy/cancelReason/lateCancel`; ProjectExpert `expertPayoutReversedAt`, `stripeTransferReversalId`, `lateCancelCallId`, `attendanceReviewPending`, `zoomAttendance`), `lib/supabase/database.types.ts` (event kinds `call_cancelled`, `no_show`), `supabase/migrations/20260910000000_call_cancelled_event.sql`, `lib/projectStore.mutateExpert(id, expertId, mutate)` (public compare-and-set; throw inside `mutate` to refuse without writing), and two CONTRACT stubs: `lib/lateCancelBilling.ts` (B2 fills; B1 calls) and `lib/expertRemoval.ts` (B1 fills; B2 calls). Keep the exported signatures of both stubs exactly; you may extend the result types additively.

All windows compare server time against `pe.booking.startUtc` (UTC ISO). Cancel window: 24 h. Late fee: 15 minutes at the agreed rates, computed ONLY through `lib/pricing.callChargeDollars(expertRate, 15)` / `expertPayoutDollars(expertRate, 15)`.

---

## Brief B1: cancel a booked call, both sides, and expert removal

WRITE: `lib/callPolicies.ts` (new), `lib/bookCall.ts`, `lib/expertRemoval.ts`, `app/api/projects/[projectId]/experts/[expertId]/booking/cancel/route.ts` (new), `app/api/schedule/[token]/route.ts`, `components/SchedulePicker.tsx`, `components/ConversationThread.tsx`, `lib/schedulingTemplates.ts`, `scripts/test-call-policies.ts` (new).
READ (not write): `lib/lateCancelBilling.ts`, `lib/outreachSuppressions.ts`, `lib/createZoomMeeting.ts`, `lib/generateIcs.ts`, `lib/sendAvailabilityRequest.ts`, `lib/emailSequence.ts`, `lib/outreachSteps.ts` (the SET NX lock pattern), `lib/projectsGuard.ts`, `lib/nudges.ts`, `app/api/projects/[projectId]/experts/[expertId]/propose-times/route.ts`, `lib/matchyBrevity.ts`.

1. `lib/callPolicies.ts`, pure, no I/O, `now` always supplied:
   - `CANCEL_WINDOW_MS = 24h`; `cancelWindow(now: number, startUtc: string): 'free' | 'late' | 'started'` ("started" when now >= start; exactly 24 h before is 'free', one ms inside is 'late'; invalid startUtc -> 'late', fail closed).
   - `lateCancelFee(expertRate: number): { clientCharge: number; expertPayout: number; minutes: 15 }` via pricing.
   - `cancelOutcome(by: 'client'|'expert'|'staff', window): { late: boolean; clientCharged: boolean; expertPaid: boolean; expertRemoved: boolean; status: 'rejected_after_outreach' }` — client late => charged+paid; expert late => removed; staff => never charged/removed; free => nothing.
2. `lib/bookCall.ts`: add `cancelCall(input: { projectId; expertId; by: 'client'|'expert'|'staff'; reason?: string; now?: number; confirmLate?: boolean })` returning a discriminated result (`ok` with the outcome, or `error` in `'not_booked' | 'already_cancelled' | 'late_not_confirmed' | 'locked' | 'conflict'`). Order: Redis `SET NX` lock `booking-lock:${projectId}:${expertId}` (EX 60, fail open like outreachSteps, release on every exit) -> `mutateExpert` that throws unless `booking` exists and is not cancelled, writes `cancelledAt/cancelledBy/cancelReason/lateCancel`, `status: 'rejected_after_outreach'`, clears `nudges` (look at how nudges.ts stops; write whatever stops them), clears `scheduling.pickTokenHash` (token invalidation) -> THEN side effects: `deleteZoomMeeting`, two cancellation emails each with its own ICS `METHOD:CANCEL` (same UID, SEQUENCE+1, via `sendBookingEmail`, read the `SendOutcome`), one Matchy thread line, `emitEngagementEvent({ type: 'call_cancelled', payload: { by, late } })`, and if `outcome.clientCharged` call `applyClientLateCancelMoney(project, pe)`; if `outcome.expertRemoved` call `removeExpertForFault(project, pe, 'late_cancel')`. A `late` client cancel with `confirmLate !== true` returns `late_not_confirmed` with the fee, BEFORE the lock. Update the header comment that says cancel is out of scope.
3. `lib/expertRemoval.ts`: implement `removeExpertForFault`: `suppress(contactEmail, 'manual', project.id)`, apology email to the project owner via `sendSequenceEmail` (copy in `schedulingTemplates.ts`, no expert or client identifiers beyond first-name style already used, <= 2 sentences, brevity-capped like other Matchy copy), thread line, status `rejected_after_outreach` via `updateExpertStatus` if not already terminal. Idempotent.
4. Client route `POST .../booking/cancel { reason?, confirmLate? }`: `guardMutatingRequest`, `getProjectForUser`, `requireProjectOwner`, same 404/403 shapes as `propose-times`. Answers `200 { ok, outcome, fee }`, `409 { error: 'late_not_confirmed', fee }`, `409 { error: 'not_booked' | 'already_cancelled' | 'conflict' }`. Also add a `GET` returning `{ window, fee }` for the confirm dialog. Response redacted through `redactExpertForViewer` if it returns the expert.
5. Expert: `POST /api/schedule/[token] { action: 'cancel', confirmLate? }` token-gated exactly like `pick` (rate limit included). Late expert cancel: the picker shows "less than 24 hours away: cancelling now removes you from ExpertMatch; ask to move instead" and requires `confirmLate`. `GET` must report `cancelled: true` afterwards and refuse further actions (`410 { error: 'booking_cancelled' }` or reuse the existing revoked-token shape; document which). `SchedulePicker.tsx` gets the Cancel action + two-step confirm.
6. `ConversationThread.tsx`: "Cancel the call" next to "Move the call"; two-step confirm that shows the GET result ("Free to cancel" or "Cancelling now charges $X (15 minutes)"), then POSTs with `confirmLate` when late. After success the thread offers "Find another time" = the existing `runProposeTimes('reschedule')` path only if the engagement can still be proposed to; DECIDE and document in your report whether a fresh engagement is needed (recommend: find-another-time runs `propose-times` and moves status back to `scheduling`; if that violates a status guard in propose-times, report it and leave the button hidden).
7. `scripts/test-call-policies.ts`: window boundaries (exactly 24 h, 1 ms inside, DST-crossing dates in UTC), fee numbers via pricing, every `cancelOutcome` branch, and `cancelCall` refusal paths using an in-memory project store if feasible (see how `test-scheduling.ts` or `test-booking-ics.ts` fake state) — at minimum the pure decisions. Must fail on old code (module missing).

Regression scripts: `test-booking-ics`, `test-scheduling`, `test-nudges`, `check-redaction`, `test-matchy-templates`, `test-brevity`, `test-expert-route-authz`.

---

## Brief B2: money for client late cancel / no-show, no-show detection

WRITE: `lib/lateCancelBilling.ts`, `app/api/webhooks/zoom/meetingEnd.ts`, `app/api/webhooks/zoom/route.ts`, `lib/createAndSendInvoice.ts`, `lib/expertPayout.ts`, `app/api/admin/attendance/route.ts` (new), `scripts/test-stripe-flows.ts`, `scripts/test-zoom-webhook.ts`.
READ: `lib/expertRemoval.ts` (call it, do not edit), `lib/pricing.ts`, `lib/chargeSavedCard.ts`, `lib/entitlements.ts`, `lib/engagementEvents.ts`, `lib/zoomLookup.ts`, `lib/auth.ts` (`adminGuard`), `app/api/webhooks/stripe/handlers.ts`.

1. `applyClientLateCancelMoney(project, pe)`: `callId = lateCancelCallId(pe)`; idempotent on `pe.lateCancelCallId === callId`; charge via `createAndSendInvoice` with 15 minutes and that callId (extend `createAndSendInvoice`'s deps/inputs minimally so a caller can pass `minutes` and a `lineLabel` such as "Late cancellation (15 min)" and so the receipt/invoice copy says so; the per-call guard must treat the id as a distinct call, which `resolveCallId(pe, passed)` already does); write `lateCancelCallId` on success (it is server-only, through `updateExpertStatus`); entitlements `canCharge` false -> `{ ok: false, reason: 'trial' }`. Payout: the existing `payment_intent.succeeded` -> `runExpertPayout` path keys on the row's billed call; make sure the payout for `${icsUid}:late-cancel` pays `expertPayoutDollars(expertRate, 15)` (extend `runExpertPayout`/`payoutCallId` so a late-cancel callId resolves to 15 minutes, never the booked duration) and that `paidCallIds` records it. No refactor beyond that.
2. Zoom: handle `meeting.participant_joined` (write `zoomAttendance.expertJoined / clientJoined` by matching `participant.email` (lower-cased) against `pe.contactEmail` and the project owner's email, or host role -> expert). Pure matcher in `meetingEnd.ts`: `classifyParticipant(participant, { expertEmail, ownerEmail }): 'expert' | 'client' | 'unknown'`. In `meeting.ended`, before billing: `resolveAttendance(pe.zoomAttendance)` pure -> `'both' | 'client_no_show' | 'expert_no_show' | 'unknown'`. `both` -> existing path unchanged. `client_no_show` -> status `rejected_after_outreach`, event `no_show { who: 'client' }`, `applyClientLateCancelMoney`; no ordinary charge. `expert_no_show` -> event `no_show { who: 'expert' }`, `removeExpertForFault(project, pe, 'no_show')`; no charge. `unknown` (no telemetry) -> write `attendanceReviewPending: true`, `recordSystemFailure({ area: 'invoice', reason: 'attendance_unconfirmed' })`, NO charge, NO completion. Rows with no `zoomAttendance` at all AND a meeting that ran at least 10 minutes may be treated as `both` ONLY if you can justify it in the report; default is `unknown`.
3. `POST /api/admin/attendance { projectId, expertId, outcome: 'both' | 'client_no_show' | 'expert_no_show' | 'neither' }` behind `adminGuard`: clears `attendanceReviewPending`, then runs the same branch the webhook would have (`both` completes and bills with the booked duration; `neither` ends the engagement with no money). Same JSON error shapes as other admin routes.
4. Tests: extend `test-zoom-webhook.ts` (participant classification, every attendance branch, unknown never bills) and `test-stripe-flows.ts` (late-cancel charge amount = `callChargeDollars(rate, 15)`, distinct callId does not trip the guard for the real call, idempotent second call, payout for late-cancel id = 15 min). Name the checks that fail before your change.

Regression: `test-billing-guard`, `test-payout-state`, `test-webhook-signature`, `test-pricing`, `test-entitlements`.

---

## Brief B3: payout clawback, champion-only first card, champion transfer

WRITE: `app/api/admin/payouts/reverse/route.ts` (new), `lib/stripeConnect.ts`, `app/admin/requests/page.tsx`, `app/api/onboarding/billing/route.ts`, `components/onboarding/BillingStep.tsx`, `app/api/org/members/route.ts`, `app/settings/team/page.tsx`, `scripts/test-auth-guards.ts`, `scripts/test-payout-state.ts`.
READ: `lib/firmStore.ts`, `lib/orgBilling.ts`, `lib/auth.ts`, `lib/engagementEvents.ts`, `app/api/webhooks/stripe/handlers.ts` (the TODO block at ~281: update its comment to point at the new route; that file is NOT on your write list — put the pointer in your report and the lead will edit it).

1. `lib/stripeConnect.reverseExpertPayout(pe, projectId, expertId, client = stripe)`: refuse if no `stripeTransferId` or already `expertPayoutReversedAt`; `client.transfers.createReversal(transferId, { metadata: { projectId, expertId } }, { idempotencyKey: 'expert-payout-reversal:' + payoutIdempotencyKey(...) })`; extend `ConnectStripeClient` for the test seam. Pure helper `canReversePayout(pe): { ok: true } | { ok: false; reason }` tested in `test-payout-state.ts`.
2. `POST /api/admin/payouts/reverse { projectId, expertId, reason }` behind `adminGuard`: writes `expertPayoutReversedAt`, `stripeTransferReversalId` via `updateExpertStatus`, `recordSystemFailure`-style system event (area `payout`, reason `payout_reversed_by_staff`), answers `200 { ok, reversalId }`, `409 { error: 'not_reversible', reason }`, `404`. Never log Stripe ids.
3. Admin console (`app/admin/requests/page.tsx`): next to each engagement's payout state show "Reverse payout" with a reason prompt, disabled when not reversible, shows reversed-at afterwards. Match existing styling; find where payout state is rendered (grep `expertPaidAt` / `stripeTransferId`); if it is not rendered on that page today, add a minimal row where the engagement/attention items are listed and say so.
4. Billing route: plain first-time POST requires `orgRole === 'org_admin'` or platform admin; otherwise `403 { error: 'champion_required', championEmail }` (look up the org's org_admin via firmStore; null if none). `alreadyComplete` short-circuit stays before the gate so members with a card on file are unaffected. `BillingStep.tsx` renders "Ask your champion (<email>) to add a card" with a continue path that does not dead-end onboarding (report which path you chose).
5. `PATCH /api/org/members { action: 'transfer_champion', email }`: allowed for current champion or platform admin; promotes target (must be an active member of the same org), demotes the caller's org's current champion(s) — if the caller is a platform admin outside the org, demote the existing champion; refuse `409 { error: 'last_org_admin' }` if the result would have zero org admins; keep the `metadata_sync_failed` warning pattern. Team page: "Make champion" now calls `transfer_champion` with a confirm ("You will become a member").
6. Tests: `test-auth-guards.ts` (champion gate decision, transfer refusals as pure functions — extract `decideChampionTransfer(...)` into a non-route module, e.g. `app/api/org/members/championTransfer.ts` which you may create), `test-payout-state.ts` (`canReversePayout`).

Regression: `test-org-billing`, `test-auth-guards`, `test-payout-state`, `test-stripe-flows`.

---

## Brief B4: Calendly hidden behind CALENDLY_ENABLED

WRITE: `lib/validateEnv.ts`, `.env.example`, `components/onboarding/CalendarStep.tsx`, `components/settings/CalendarPanel.tsx`, `app/api/onboarding/calendar/route.ts`, `lib/calendarConnections.ts`, `scripts/test-availability-windows.ts`, `lib/calendlyFlag.ts` (new, pure: `calendlyEnabled(env = process.env): boolean` true only for `'true'`).
READ: `scripts/check-env-drift.ts`, `lib/fetchCalendlySlots.ts` (do NOT touch), `app/api/settings/*` (whatever reports calendar status to the panel).

1. `CALENDLY_ENABLED` in `OPTIONAL_VARS` with a comment, and in `.env.example`; `npx tsx scripts/check-env-drift.ts` must stay green.
2. Flag off: `CalendarStep` and `CalendarPanel` do not offer Calendly (the component needs the flag from the server — use whatever mechanism these components already use for server config, or a `NEXT_PUBLIC_`-free approach such as the status endpoint returning `calendlyEnabled`; do not add a NEXT_PUBLIC var); `POST /api/onboarding/calendar` answers `400 { error: 'calendly_disabled' }` for `provider: 'calendly'`; `getClientSlotsForUser` returns `[]`-equivalent "not connected" for an existing Calendly row (and `isCalendarConnected` / `connectionIsUsable` say false for calendly when disabled, so the user is prompted to connect Google or type hours). Flag on: behaviour unchanged.
3. `test-availability-windows.ts`: flag parsing, `connectionIsUsable` with the flag on and off (pass env explicitly). Name the check that fails before.

Regression: `check-env-drift`, `test-availability-windows`, `test-scheduling`, `test-freebusy-inversion`.
